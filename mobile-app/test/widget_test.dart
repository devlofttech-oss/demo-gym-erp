// Basic smoke test placeholder. UI is verified by running the app against
// Firebase; this keeps `flutter test` green without network access.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
